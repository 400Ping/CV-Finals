# Test Labels

Each `.txt` file uses YOLO bounding-box format. Class `0` is the drink cup.

Annotate the independent test images from the project root:

```bash
python scripts/annotate.py --images dataset/test/images --labels dataset/test/labels
```
